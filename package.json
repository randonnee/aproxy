{
  "name": "aproxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "dev:ui": "bun run --cwd ui dev",
    "build:ui": "bun run --cwd ui build",
    "start": "bun src/index.ts",
    "go": "bun run --cwd ui build && bun src/index.ts",
    "bench": "bun run scripts/bench.ts",
    "desktop": "bun run scripts/build-ui.ts && bun build src/electrobun/index.ts --outdir build/dev-macos-arm64/Aproxy-dev.app/Contents/Resources/app/bun --target bun && electrobun dev",
    "desktop:build": "electrobun build --env=stable"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  },
  "dependencies": {
    "effect": "latest",
    "electrobun": "^1.13.1"
  }
}
